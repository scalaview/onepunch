<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0" />
  <title>{{#if title }}{{title}}(星客万维){{else}}星客万维{{/if}}</title>
  {{{css "main"}}}
</head>
<body>
    {{{js "main" }}}
    {{{body}}}
    {{> bottom-menu }}
</body>
</html>
